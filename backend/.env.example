# ============================================
# LexiCraft Backend Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# ============================================
# Database Configuration (REQUIRED)
# ============================================
# PostgreSQL connection string (Supabase)
# Get from: Supabase Dashboard → Settings → Database → Connection string
# Use Transaction mode (port 6543) for connection pooling
#
# Development: Use dev Supabase project connection string
# Production: Use production Supabase project connection string
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:6543/postgres?sslmode=require

# ============================================
# Supabase Authentication (REQUIRED)
# ============================================
# JWT Secret for verifying Supabase authentication tokens
# Get from: Supabase Dashboard → Settings → API → JWT Secret (Legacy)
#
# Development: Use dev Supabase project JWT secret
# Production: Use production Supabase project JWT secret
SUPABASE_JWT_SECRET=your-super-secret-jwt-token-with-at-least-32-characters-long

# Optional: Supabase URL (if needed for direct Supabase API calls)
# Usually not required, but some scripts may use it
SUPABASE_URL=https://your-project-ref.supabase.co

# ============================================
# Neo4j Graph Database (REQUIRED)
# ============================================
# Neo4j connection details for vocabulary graph
#
# Development: Use local Neo4j or dev Neo4j Aura instance
# Production: Use production Neo4j Aura instance
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your-neo4j-password

# ============================================
# CORS Configuration (Production Only)
# ============================================
# Allowed origins for CORS (comma-separated)
# Development: Not needed (uses "*" for local development)
# Production: Specify exact frontend URLs
CORS_ORIGINS=https://lexicraft.xyz,https://www.lexicraft.xyz

# ============================================
# Server Configuration
# ============================================
# Port number (usually set automatically by Railway/Render)
# Only set if running locally with custom port
PORT=8000

# ============================================
# AI/LLM API Keys (Optional - for vocabulary enrichment)
# ============================================
# These are used by backend scripts for vocabulary enrichment
# Not required for basic API functionality

# Google Gemini API (for vocabulary enrichment)
GOOGLE_API_KEY=your-google-api-key
# Alternative name (some scripts use this)
GEMINI_API_KEY=your-google-api-key

# Anthropic Claude API (alternative LLM)
ANTHROPIC_API_KEY=your-anthropic-api-key

# OpenAI API (for audio generation)
OPENAI_API_KEY=your-openai-api-key

# ============================================
# Debug/Development Flags
# ============================================
# Enable debug logging for learner pipeline
# Set to "true" to enable verbose logging
DEBUG_LEARNER_PIPELINE=false

# ============================================
# Environment Notes
# ============================================
#
# Development (.env):
# - Use dev Supabase project (DATABASE_URL, SUPABASE_JWT_SECRET)
# - Use local Neo4j or dev Neo4j instance
# - CORS_ORIGINS not needed (uses "*")
# - PORT=8000 for local development
#
# Production (Railway):
# - Use production Supabase project (DATABASE_URL, SUPABASE_JWT_SECRET)
# - Use production Neo4j Aura instance
# - CORS_ORIGINS=https://lexicraft.xyz,https://www.lexicraft.xyz
# - PORT set automatically by Railway
#
# See README_ENV.md for complete environment setup guide.
